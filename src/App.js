import './App.css';
import mockdata from './mock-data.json';

function App() {
  console.log(mockdata);
    return (
      <div className = "App">
        Banner
        Searchbar
        CityFilter
        CardComponent
        MapComponent

        Router
        Landingpage
        Detailpage
        Router
      </div>
    );
}

export default App;
